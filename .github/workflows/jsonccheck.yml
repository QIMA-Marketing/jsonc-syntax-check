name: JSON check

on:
  push:
#    paths:
#      - '**.jsonc'
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: jsonc-syntax-check
        uses: stevieb9/jsonc-syntax-check@v0.01
        with:
          pattern: "\\.jsonc$"